extends layout.pug
include issues/common.pug

block body
    ul.issues-list
        each thing in things
            +activitySpecified(thing, thing.issue)
append scripts
    script(src=vroot+"static/expandable.js")
